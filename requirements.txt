# Runtime dependencies (deployed to Render)
pyyaml>=6.0
streamlit>=1.32
vininfo>=1.7.0
plotly>=5.14

# Dev / test dependencies (install locally with: pip install -r requirements-dev.txt)
# NOT installed on Render — keep this file lean
